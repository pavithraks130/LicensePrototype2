@model List<License.MetCalWeb.Models.PurchaseOrder>
@{
    ViewBag.Title = "OrderStatus";
}

<h2>OrderStatus</h2>
<br />
<br />
@if (Model.Count == 0)
{
    <div class="row">
        <div class="col-md-12">
            No order Exist
        </div>
    </div>
}
else
{
    <div class="row">
        <div class="col-md-12">
            <table class="table">
                <tr>
                    <th class="col-md-4">
                        Purcchase Order No
                    </th>
                    <th class="col-md-4">
                        Purchase Order Status
                    </th>
                    <th class="col-md-4">
                        Approved/Rejected By
                    </th>
                </tr>
                @for (int i = 0; i < Model.Count; i++)
                {
                    <tr>
                        <td class="col-md-4">
                            @Html.ActionLink(Model[i].PurchaseOrderNo, "OrderDetail", "PurchaseOrder", new { id = Model[i].Id }, null)
                        </td>
                        <td class="col-md-4">
                            @if (String.IsNullOrEmpty(Model[i].ApprovedBy))
                            {
                                <label>Pending</label>
                            }
                            else if (Model[i].IsApproved)
                            {
                                <label>Approved</label>
                            }
                            else
                            {
                                <label>Rejected</label>
                            }
                        </td>
                        <td class="col-md-4">
                            @Model[i].ApprovedBy
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
}